= render "projects/sub_navbar"

.row
  .col-md-5
    = render "index"

  .col-md-7
    %h3
      .row
        .col-md-9
          - if @document
            = "正在编辑：#{@document.title}"
        .col-md-3
    %br

    = render "form"

