%h1.page-header 全部日报

= render "tab_bar"

%br

.row
  .col-md-9
    %table.table.table-striped
      %tr
        %th 内容
        %th 用户
        %th 日期
        %th 创建
        %th 操作
    
      - @dailies.each do |daily|
        %tr
          %td= daily.content
          %td= daily.user
          %td= daily.date
          %td= daily.created_at
          %td
            = link_to 'Show', daily
            = link_to 'Edit', edit_daily_path(daily)
            = link_to 'Destroy', daily, :method => :delete, :data => { :confirm => 'Are you sure?' }

  .col-md-2
    #calendar-container

    <script type="text/javascript">
    $('#calendar-container').datepicker({
    weekStart: 1,
    language: "zh-CN",
    todayHighlight: true
    }).on('changeDate', function(e){
    var clickDate = e.format("yyyy-mm-dd");
    window.location.href="/dailies/my/" + clickDate; 
    });
    </script>

%br
%br

= render 'form'

